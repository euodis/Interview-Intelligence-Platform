version: '3.8'

services:
  web:
    image: node:20
    container_name: ii_platform_web
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    command: npm run dev
    environment:
      - NODE_ENV=development
